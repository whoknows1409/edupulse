apiVersion: v1
kind: Service
metadata:
  name: edupulse-postgres
  namespace: edupulse
spec:
  selector:
    app: edupulse-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP
